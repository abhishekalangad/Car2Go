# CAR2GO - DATA FLOW DIAGRAMS

## 🔄 COMPLETE SYSTEM DATA FLOW

```
┌─────────────────────────────────────────────────────────────────────┐
│                         CAR2GO SYSTEM                               │
│                                                                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│  │  ADMIN   │  │   USER   │  │  DRIVER  │  │ SERVICE  │          │
│  │          │  │          │  │          │  │  CENTER  │          │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘          │
│       │             │             │             │                 │
│       └─────────────┴─────────────┴─────────────┘                 │
│                     │                                              │
│              ┌──────▼───────┐                                      │
│              │ LOGIN TABLE  │ (Central Auth)                       │
│              │  l_id (PK)   │                                      │
│              │  l_uname     │                                      │
│              │  l_password  │                                      │
│              │  l_type      │                                      │
│              │  l_approve   │                                      │
│              └──────┬───────┘                                      │
│                     │                                              │
│        ┌────────────┼────────────┬─────────────┬────────────┐     │
│        │            │            │             │            │     │
│   ┌────▼────┐  ┌───▼────┐  ┌───▼────┐   ┌────▼─────┐ ┌───▼──┐  │
│   │user_reg │  │driver_ │  │service_│   │ emp_reg  │ │ rent │  │
│   │         │  │  reg   │  │  reg   │   │          │ │      │  │
│   │(Users)  │  │(Drivers│  │(Service│   │(Employee)│ │(Cars)│  │
│   │         │  │  )     │  │Centers)│   │          │ │      │  │
│   └────┬────┘  └───┬────┘  └───┬────┘   └──────────┘ └──┬───┘  │
│        │           │           │                          │      │
│        │           │           │                          │      │
│   ┌────▼──────┬────▼─────┬─────▼──────┐             ┌───▼──────┐│
│   │ bookcar   │bookdriver│bookservice │             │ bservice ││
│   │           │          │            │             │          ││
│   │(Car Rent) │(Driver   │(Service    │             │(Special  ││
│   │           │ Booking) │ Booking)   │             │ Service) ││
│   └──────┬────┴──────────┴────────────┘             └──────────┘│
│          │                                                        │
│   ┌──────▼────────┐  ┌─────────────┐  ┌─────────────┐           │
│   │   rating      │  │   drating   │  │   srating   │           │
│   │ (Car Ratings) │  │(Driver Rate)│  │(Service Rate│           │
│   └───────────────┘  └─────────────┘  └─────────────┘           │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🚗 CAR RENTAL FLOW

```
USER REGISTRATION
    │
    ▼
┌─────────────────┐
│ userreg.php     │
│ Input: name,    │
│ email, password,│
│ address, phone, │
│ pincode, license│
└────────┬────────┘
         │ INSERT
         ▼
┌─────────────────┐      ┌─────────────────┐
│ login TABLE     │      │ user_reg TABLE  │
│ l_uname=email   │──────│ ul_id=l_id      │
│ l_password=pass │      │ u_name, u_email │
│ l_type='user'   │      │ u_address, etc. │
│ l_approve=      │      └─────────────────┘
│  'approve'      │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ LOGIN PAGE      │
│ login.php       │
│ Check: l_uname  │
│   & l_password  │
└────────┬────────┘
         │ SESSION l_id
         ▼
┌─────────────────┐
│ userprofile.php │
│ User Dashboard  │
└────────┬────────┘
         │
    ┌────┴────┬─────────┬──────────┐
    │         │         │          │
ADD CAR    SEARCH    BOOK      BOOK
TO RENT     CARS    DRIVER   SERVICE
    │         │         │          │
    ▼         ▼         ▼          ▼


ADD CAR TO RENT FLOW:
──────────────────────
┌─────────────────────┐
│ rentingform.php     │
│ Input: company,     │
│ model, year, number,│
│ rent/km, seats,     │
│ photos, docs        │
└──────────┬──────────┘
           │ INSERT
           ▼
┌──────────────────────┐
│ rent TABLE           │
│ rl_id = user's l_id  │
│ r_company, r_mname   │
│ r_year, r_number     │
│ r_ppkm, r_seat       │
│ r_status='not approve│
│ r_car, r_insurance   │
│ r_tax, r_polution    │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ ADMIN APPROVAL       │
│ adminviewcar.php     │
│ Admin sets:          │
│ r_status='approved'  │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ CAR NOW VISIBLE      │
│ viewcars.php         │
│ Users can search     │
└──────────────────────┘


SEARCH & BOOK CAR FLOW:
────────────────────────
┌─────────────────────┐
│ viewcars.php        │
│ Input: km radius    │
└──────────┬──────────┘
           │ SELECT WHERE
           │ pincode BETWEEN
           │ (user_pincode-km)
           │ AND (user_pincode+km)
           ▼
┌──────────────────────┐
│ Display Cars List    │
│ - Car image          │
│ - Company, model     │
│ - Seats, rent/km     │
│ - Average rating     │
└──────────┬───────────┘
           │ User clicks
           │ "View Car"
           ▼
┌──────────────────────┐
│ viewcarprofile.php   │
│ Show full details:   │
│ - All car info       │
│ - Owner contact      │
│ - Reviews            │
│ - Documents          │
└──────────┬───────────┘
           │ User clicks
           │ "Book This Car"
           ▼
┌──────────────────────┐
│ urenthis.php         │
│ Input: start_date    │
│        end_date      │
└──────────┬───────────┘
           │ INSERT
           ▼
┌──────────────────────┐
│ bookcar TABLE        │
│ bo_id = user's l_id  │
│ br_id = car's r_id   │
│ b_day1 = start_date  │
│ b_day2 = end_date    │
│ b_status = 'Booked'  │
│ payment = NULL       │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ CAR OWNER NOTIFIED   │
│ (via their dashboard)│
│ Views: urenthis1.php │
└──────────┬───────────┘
           │ Owner confirms
           ▼
┌──────────────────────┐
│ UPDATE bookcar       │
│ SET b_status=        │
│   'confirmed'        │
│ SET payment=amount   │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ RENTAL CONFIRMED     │
│ User picks up car    │
│ Uses car             │
│ Returns car          │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ rating.php           │
│ User rates car       │
│ Input: rating (1-5)  │
│        review text   │
└──────────┬───────────┘
           │ INSERT
           ▼
┌──────────────────────┐
│ rating TABLE         │
│ rating = stars       │
│ review = text        │
│ l_id = user's l_id   │
│ ur_id = owner's l_id │
└──────────────────────┘
```

---

## 👨‍✈️ DRIVER BOOKING FLOW

```
DRIVER REGISTRATION
    │
    ▼
┌─────────────────────┐
│ driverreg.php       │
│ Input: name, email, │
│ password, address,  │
│ phone, pincode,     │
│ license, ID proof,  │
│ daily_charge        │
└──────────┬──────────┘
           │ INSERT
           ▼
┌──────────────────────┐     ┌──────────────────────┐
│ login TABLE          │     │ driver_reg TABLE     │
│ l_uname=email        │─────│ dl_id = l_id         │
│ l_password=pass      │     │ d_name, d_email      │
│ l_type='driver'      │     │ d_address, d_pincode │
│ l_approve='approve'  │     │ d_licence, d_proof   │
└──────────────────────┘     │ d_amount = charge/day│
                             └──────────┬───────────┘
                                        │
                                        ▼
                             ┌──────────────────────┐
                             │ DRIVER PROFILE       │
                             │ driverprofile.php    │
                             │ Dashboard            │
                             └──────────────────────┘


USER SEARCHES DRIVER:
─────────────────────
┌─────────────────────┐
│ viewdriv.php        │
│ viewdriv1.php       │
│ Search by pincode   │
└──────────┬──────────┘
           │ SELECT WHERE
           │ d_pincode BETWEEN
           │ range
           ▼
┌──────────────────────┐
│ Display Drivers      │
│ - Photo              │
│ - Name               │
│ - Charge per day     │
│ - Rating             │
└──────────┬───────────┘
           │ User clicks
           │ "View Driver"
           ▼
┌──────────────────────┐
│ viewdriverprofile.php│
│ Full driver details  │
│ Contact info         │
│ Reviews              │
└──────────┬───────────┘
           │ User clicks
           │ "Book Driver"
           ▼
┌──────────────────────┐
│ udriverthis.php      │
│ udriverthis2.php     │
│ Input: start_date    │
│        end_date      │
└──────────┬───────────┘
           │ INSERT
           ▼
┌──────────────────────┐
│ bookdriver TABLE     │
│ dr_id = user's l_id  │
│ dd_id = driver dl_id │
│ d_day1 = start_date  │
│ d_day2 = end_date    │
│ d_status = 'Booked'  │
│ payment = NULL       │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ DRIVER NOTIFIED      │
│ ubookdriver.php      │
│ (Driver dashboard)   │
└──────────┬───────────┘
           │ Driver confirms
           ▼
┌──────────────────────┐
│ UPDATE bookdriver    │
│ SET d_status=        │
│   'confirmed'        │
│ SET payment=         │
│   (days * d_amount)  │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ SERVICE COMPLETED    │
│ bdconfirmed.php      │
│ bdcompleted.php      │
│ bdongoing.php        │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ ratingd.php          │
│ User rates driver    │
│ INSERT into drating  │
└──────────────────────┘
```

---

## 🔧 SERVICE BOOKING FLOW

```
SERVICE CENTER REGISTRATION
    │
    ▼
┌─────────────────────┐
│ servicereg.php      │
│ Input: name, email, │
│ password, address,  │
│ phone, pincode,     │
│ license, RC book    │
└──────────┬──────────┘
           │ INSERT
           ▼
┌──────────────────────┐     ┌──────────────────────┐
│ login TABLE          │     │ service_reg TABLE    │
│ l_uname=email        │─────│ sl_id = l_id         │
│ l_password=pass      │     │ s_name, s_email      │
│ l_type='service      │     │ s_address, s_pincode │
│   center'            │     │ s_licence, s_rc      │
│ l_approve='approve'  │     └──────────┬───────────┘
└──────────────────────┘                │
                                        ▼
                             ┌──────────────────────┐
                             │ SERVICE CENTER       │
                             │ DASHBOARD            │
                             │ serviceprofile.php   │
                             └──────────┬───────────┘
                                        │
                                        ▼
                             ┌──────────────────────┐
                             │ ADD SERVICES         │
                             │ serviceform.php      │
                             │ Input: service_name  │
                             │        description   │
                             │        price         │
                             └──────────┬───────────┘
                                        │ INSERT
                                        ▼
                             ┌──────────────────────┐
                             │ service_details TABLE│
                             │ sel_id = service's   │
                             │         l_id         │
                             │ se_name = name       │
                             │ se_details = desc    │
                             │ se_price = price     │
                             └──────────────────────┘


USER BOOKS SERVICE - TWO PATHS:
────────────────────────────────

PATH 1: GENERAL SERVICE (Predefined)
─────────────────────────────────────
┌─────────────────────┐
│ viewservicee1.php   │
│ User views service  │
│ centers             │
└──────────┬──────────┘
           ▼
┌─────────────────────┐
│ viewserviceprofile  │
│ .php                │
│ View services offered│
└──────────┬──────────┘
           │ User selects
           │ a service
           ▼
┌─────────────────────┐
│ servicereq.php      │
│ Select service      │
│ Select date         │
└──────────┬──────────┘
           │ INSERT
           ▼
┌─────────────────────┐
│ bookservice TABLE   │
│ br_id = user's l_id │
│ bs_id = service_id  │
│ b_date = date       │
│ b_status = NULL     │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ ADMIN APPROVAL      │
│ approveservice.php  │
│ rejectservice.php   │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ UPDATE bookservice  │
│ SET b_status=       │
│   'approved' OR     │
│   'rejected'        │
└─────────────────────┘


PATH 2: SPECIALIZED SERVICE (Custom)
─────────────────────────────────────
┌─────────────────────┐
│ viewser.php         │
│ User describes      │
│ car issue           │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ dedicateduview.php  │
│ Input: car_id       │
│        issue_desc   │
└──────────┬──────────┘
           │ INSERT
           ▼
┌─────────────────────┐
│ bservice TABLE      │
│ cs_uid = user's l_id│
│ cs_cid = car's r_id │
│ cs_date = date      │
│ cs_ureview = issue  │
│ cs_edate = NULL     │
│ cs_ereview = NULL   │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ EMPLOYEE RESPONDS   │
│ (employee portal)   │
│ INPUT: response     │
└──────────┬──────────┘
           │ UPDATE
           ▼
┌─────────────────────┐
│ UPDATE bservice     │
│ SET cs_edate = date │
│ SET cs_ereview =    │
│     response        │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ USER SEES RESPONSE  │
│ uviewservh.php      │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ SERVICE COMPLETED   │
│ User rates          │
│ ratings.php         │
│                     │
│ INSERT into srating │
└─────────────────────┘
```

---

## 👤 ADMIN WORKFLOW

```
┌─────────────────────┐
│ ADMIN LOGIN         │
│ login.php           │
│ l_type = 'admin'    │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ adminprofile.php    │
│ ADMIN DASHBOARD     │
└──────────┬──────────┘
           │
    ┌──────┴──────┬───────────┬────────────┬──────────┐
    │             │           │            │          │
    ▼             ▼           ▼            ▼          ▼
┌────────┐  ┌──────────┐  ┌─────────┐  ┌────────┐  ┌────────┐
│ USERS  │  │ DRIVERS  │  │ SERVICE │  │  CARS  │  │EMPLOYEE│
│        │  │          │  │ CENTERS │  │        │  │        │
└────┬───┘  └────┬─────┘  └────┬────┘  └───┬────┘  └───┬────┘
     │           │             │            │           │
     ▼           ▼             ▼            ▼           ▼
┌────────┐  ┌──────────┐  ┌─────────┐  ┌────────┐  ┌────────┐
│viewuser│  │adminview │  │adminview│  │adminview  │viewemp │
│.php    │  │driver.php│  │service  │  │car.php │  │.php    │
│        │  │          │  │.php     │  │        │  │        │
│View All│  │View All  │  │View All │  │View All│  │View All│
│Users   │  │Drivers   │  │Service  │  │Cars    │  │Employ  │
└────────┘  └────┬─────┘  │Centers  │  └───┬────┘  │ees     │
                 │         └────┬────┘      │       └───┬────┘
                 │              │           │           │
          ┌──────┴──────┐  ┌───┴────┐  ┌───┴────┐  ┌───┴────┐
          │ APPROVE/    │  │APPROVE/│  │APPROVE/│  │ DELETE │
          │ DISAPPROVE  │  │DISAPPR │  │DISAPPR │  │        │
          │             │  │        │  │        │  │        │
          │appser1.php  │  │appser2 │  │Directly│  │deleteemp
          │disser1.php  │  │.php    │  │in page │  │.php    │
          └─────────────┘  │disser2 │  └────────┘  └────────┘
                           │.php    │
                           └────────┘


SERVICE BOOKING APPROVAL:
─────────────────────────
┌─────────────────────┐
│ Admin views pending │
│ service bookings    │
└──────────┬──────────┘
           │
    ┌──────┴──────┐
    │             │
    ▼             ▼
┌────────┐  ┌──────────┐
│APPROVE │  │ REJECT   │
│        │  │          │
│approve │  │reject    │
│service │  │service   │
│.php    │  │.php      │
└───┬────┘  └────┬─────┘
    │            │
    └──────┬─────┘
           ▼
┌─────────────────────┐
│ UPDATE bookservice  │
│ SET b_status =      │
│  'approved' or      │
│  'rejected'         │
└─────────────────────┘
```

---

## 📊 RATING SYSTEM DATA FLOW

```
AFTER SERVICE COMPLETION
    │
    ▼
┌─────────────────────────────────────┐
│ USER RATES SERVICE/CAR/DRIVER       │
└─────────────────┬───────────────────┘
                  │
         ┌────────┴────────┬────────────┐
         │                 │            │
         ▼                 ▼            ▼
    ┌────────┐        ┌────────┐   ┌────────┐
    │ CAR    │        │ DRIVER │   │SERVICE │
    │        │        │        │   │ CENTER │
    └───┬────┘        └───┬────┘   └───┬────┘
        │                 │            │
        ▼                 ▼            ▼
┌───────────────┐  ┌──────────────┐  ┌──────────────┐
│ rating.php    │  │ ratingd.php  │  │ ratings.php  │
│               │  │              │  │              │
│ Input:        │  │ Input:       │  │ Input:       │
│ - rating(1-5) │  │ - rating(1-5)│  │ - rating(1-5)│
│ - review text │  │ - review text│  │ - review text│
└───────┬───────┘  └──────┬───────┘  └──────┬───────┘
        │                 │                  │
        │ INSERT          │ INSERT           │ INSERT
        ▼                 ▼                  ▼
┌───────────────┐  ┌──────────────┐  ┌──────────────┐
│ rating TABLE  │  │ drating TABLE│  │ srating TABLE│
│               │  │              │  │              │
│ re_id (PK)    │  │ re_id (PK)   │  │ se_id (PK)   │
│ rating        │  │ rating       │  │ rating       │
│ review        │  │ review       │  │ review       │
│ l_id (rater)  │  │ i_id (rater) │  │ u_id (rater) │
│ ur_id (rated) │  │ ld_id (rated)│  │ sl_id (rated)│
└───────────────┘  └──────────────┘  └──────────────┘
```

---

## 🔄 SESSION MANAGEMENT

```
┌─────────────────────┐
│ USER LOGS IN        │
│ login.php           │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────────────┐
│ VALIDATE CREDENTIALS        │
│ SELECT * FROM login         │
│ WHERE l_uname = email       │
│   AND l_password = pass     │
└──────────┬──────────────────┘
           │
           ▼
┌─────────────────────────────┐
│ SET SESSION                 │
│ $_SESSION['l_id'] = l_id    │
└──────────┬──────────────────┘
           │
           ▼
┌─────────────────────────────┐
│ REDIRECT BASED ON l_type    │
│                             │
│ if (l_type == 'admin')      │
│   -> adminprofile.php       │
│ if (l_type == 'user')       │
│   -> userprofile.php        │
│ if (l_type == 'driver')     │
│   -> driverprofile.php      │
│ if (l_type == 'service      │
│     center')                │
│   -> serviceprofile.php     │
│ if (l_type == 'employe')    │
│   -> employee/empprofile.php│
└──────────┬──────────────────┘
           │
           ▼
┌─────────────────────────────┐
│ ALL SUBSEQUENT REQUESTS     │
│ VERIFY: if(isset($_SESSION  │
│         ['l_id']))          │
│ Use l_id for all queries    │
└─────────────────────────────┘
```

---

## 🗄️ DATABASE RELATIONSHIPS DETAILED

```
                    ┌──────────────────┐
                    │   login (CORE)   │
                    │   ──────────────  │
                    │   l_id (PK)      │
                    │   l_uname        │
                    │   l_password     │
                    │   l_type         │
                    │   l_approve      │
                    └────┬─────────────┘
                         │
        ┌────────────────┼─────────────────┬──────────────┐
        │                │                 │              │
        │ FK: ul_id      │ FK: dl_id       │ FK: sl_id    │ FK: rl_id
        │                │                 │              │
┌───────▼────────┐ ┌────▼────────┐  ┌─────▼──────┐  ┌───▼─────┐ ┌─────────┐
│   user_reg     │ │  driver_reg │  │service_reg │  │  rent   │ │ emp_reg │
│   ──────────   │ │  ──────────  │  │──────────  │  │──────── │ │─────────│
│   u_id (PK)    │ │  d_id (PK)   │  │ s_id (PK)  │  │r_id (PK)│ │e_id (PK)│
│   ul_id (FK)───┼─┼──dl_id (FK)──┼──┼─sl_id (FK)─┼──┼─rl_id   │ │el_id(FK)│
│   u_name       │ │  d_name      │  │ s_name     │  │(FK)     │ │e_name   │
│   ...          │ │  d_amount    │  │ ...        │  │r_company│ │...      │
└────────────────┘ │  ...         │  └────────────┘  │r_mname  │ └─────────┘
                   └───────┬──────┘                  │r_status │
                           │                         │...      │
                           │                         └────┬────┘
                           │                              │
                    ┌──────┴────────┐              ┌──────┴───────┐
                    │               │              │              │
             ┌──────▼──────┐  ┌─────▼──────┐  ┌───▼────────┐ ┌──▼───────┐
             │  bookdriver │  │  drating   │  │  bookcar   │ │bservice  │
             │  ──────────  │  │────────────│  │──────────  │ │──────────│
             │  d_id (PK)   │  │re_id (PK)  │  │b_id (PK)   │ │cs_id (PK)│
             │  dr_id (FK)──┼──┼─i_id (FK)  │  │bo_id (FK)──┼─┼cs_uid(FK│
             │  dd_id (FK)──┼──┼─ld_id (FK)─┼──┼─br_id (FK)─┼─┼─cs_cid   │
             │  d_day1,2    │  │rating      │  │b_day1,2    │ │(FK)     │
             │  d_status    │  │review      │  │b_status    │ │cs_ureview
             │  payment     │  └────────────┘  │payment     │ │cs_ereview
             └──────────────┘                  └──────┬─────┘ │...      │
                                                      │       └─────────┘
                                                      │
                                              ┌───────▼───────┐
                                              │   rating      │
                                              │   ────────────│
                                              │   re_id (PK)  │
                                              │   l_id (FK)───┼─→ login
                                              │   ur_id (FK)──┼─→ login
                                              │   rating      │
                                              │   review      │
                                              └───────────────┘


SERVICE CENTER BRANCH:
──────────────────────
┌─────────────────┐
│  service_reg    │
│  ──────────────  │
│  s_id (PK)      │
│  sl_id (FK)─────┼─→ login
│  ...            │
└────┬────────────┘
     │
     ▼
┌─────────────────┐
│service_details  │
│────────────────  │
│se_id (PK)       │
│sel_id (FK)──────┼─→ service_reg.sl_id
│se_name          │
│se_details       │
│se_price         │
└────┬────────────┘
     │
     ▼
┌─────────────────┐    ┌─────────────────┐
│ bookservice     │    │   srating       │
│────────────────  │    │────────────────  │
│b_id (PK)        │    │se_id (PK)       │
│br_id (FK)───────┼─→  │u_id (FK)────────┼─→ login
│bs_id (FK)───────┼─→  │sl_id (FK)───────┼─→ service_reg
│b_date           │    │rating           │
│b_status         │    │review           │
└─────────────────┘    └─────────────────┘
```

---

**END OF DATA FLOW DOCUMENTATION**
